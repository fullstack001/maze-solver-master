from setuptools import setup, find_packages

setup(
    name='maze-solver',
    version='1.0.0',
    packages=find_packages,
    install_requires=[],
    entry_points={},
    author='Richard Sefton',
    author_email='richard.sefton@googlemail.com',
    description='Simple project to find a path through a maze',
    url='',
    classifiers=[]
)